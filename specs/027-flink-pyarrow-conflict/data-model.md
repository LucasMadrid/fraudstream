# Data Model: Document & Contain `apache-flink` ↔ `analytics` pyarrow Conflict (#31)

**Phase 1 output** | Branch: `027-flink-pyarrow-conflict` | Date: 2026-05-27

## Entities

**Not applicable.** This feature is documentation-only. It introduces no runtime entities, no schema changes, no persisted state, no events, and no API surfaces.

The only "model" elements involved are the **documentation artifacts** being produced (an ADR, a README section, a `SECURITY.md`, a `uv.lock` header), and those are content, not entities.
