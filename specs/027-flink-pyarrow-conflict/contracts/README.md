# Contracts: Document & Contain `apache-flink` ↔ `analytics` pyarrow Conflict (#31)

**Phase 1 output** | Branch: `027-flink-pyarrow-conflict`

## No external interface changes

This feature is documentation-only. No public APIs, no Kafka schemas, no HTTP routes, no CLI commands, and no environment-variable contracts are added, removed, or modified.

Consumers downstream of this PR will see new and updated **Markdown files** only. There is no API to define, no contract to test.
